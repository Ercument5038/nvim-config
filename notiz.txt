1 1 1 2 1 3 1 4 v-block g(ctrl-a)

q: for normal command line editin or in command line ctrl-f to open the command
line window

gx für https://www.google.com gf für files

alt + motion zb o in insert mode spart esc und wieder insert oder alt + p

alles auswählen dann = machen für indenting

gv to reselect last visual selection

g_ end of the line excluding new line character

ctrl-n für wort autocompletion builtin ctrl-x dann ctrl+f für filename/path
autocompletion builtin

gw in visual mode to wrap long lines of text

o in visual mode jumps to the other side of the selection

gi goes to the last place you inserted text in the current file

. to repeat last edit motions

ctrl-a if unmapped incremets the number

ctrl-] Jump to the definition of the keyword under the cursor or shift k in
help

m[x] wo x register is setzt marks dort, mit '[x] geht man dorthin m[X] wo X
register is setzt globale makrs dort, mit '[X] geht man dorthin

cht.sh für cheatsheets

:vsplit term://pwsh für terminal ctrl-\ ctrl+n für normal mode in terminal

<c-w>v oder <c-w>s für splitwindow vertical horizontal oder :sp oder :vs <c-w>
= um alle spilts gleiche groß zu machen

:setlocal spell spelllang=en_us enables spellcheck. ]s and [s navigate through
misspelled words. z= suggests corrections, zg adds words, zw marks words as
incorrect. :set nospell disables spellcheck.

vib for visual inner block ( ) and viB for visual inner block { adkflöjsdflk}

Object selection w = ein word W = bis nächste leerzeichen s = ganze satz p =
paragraph also mehrere zeilen voon text b = ( ) B = { } t = tag also <as> </as>

ctrl+ü bzw. ctrl+[ für normal mode

ctrl+u in insert mode to delete everything, good for indenting

g== in help file um code blöcke auszuführen

:set shiftwidth=2 to change tapstop

gd um zur definition zu springen

gm to go horizontally to the middle of the screen gM to go horizontally to the
middle of the text

use :verbose set shiftwidth? to get which option or what set the option 

:cd %:h to change direcotry of the current file

vimdiff mode
vim/nvim -d und datein dann diffmode
